export const PROJECT_NAME_PLACEHOLDER = "New Project";

export const PROJECT_NAME_MODEL = "gpt-4o-mini";

export const PROJECT_NAME_PROMPT = `You are a helpful assistant that creates concise, descriptive software project names. Respond with a short name of at most six words that captures the user's intent. Avoid quotation marks and punctuation other than spaces.`;
