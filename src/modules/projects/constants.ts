export const PROJECT_NAME_PLACEHOLDER = "New Project";

export const PROJECT_NAME_MODEL = "gpt-4o-mini";

export const PROJECT_NAME_PROMPT = `You are a helpful assistant that creates concise, descriptive software project names. Respond with an elegant, memorable name containing no more than four words that captures the user's intent. Avoid quotation marks and punctuation other than spaces.`;
